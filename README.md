## Linting

To lint all TypeScript files in the project:

```bash
npx eslint "**/*.ts"
```

After completing the test, run ESLint on all test files:

```bash
npx eslint "tests/**/*.ts"
```

Command to run prettierrc file to format files automatically

```bash
npx prettier --write .
```
